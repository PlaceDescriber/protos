syntax = "proto3";

package common;

enum MapType {
  SATELLITE = 0;
  PLAN = 1;
  HYBRID = 2;
}

message Point {
  double latitude = 1;
  double longitude = 2;
}

message Polygon {
  repeated Point points = 1;
}
